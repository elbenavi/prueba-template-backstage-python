apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.repoName}}
  description: Python app that displays time - named ${{values.repoName}}
  annotations:
   dev.azure.com/project-repo: enerbit/_git/${{values.repoName}}
   backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: development
  lifecycle: experimental